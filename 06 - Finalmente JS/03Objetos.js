//Object
const person = {
    name: 'Gabriel',
    age: 25,
    weight: 88.6,
    isAdmin: true
       
} /*com o Objeto person para transmitir a tela usei console log com interpolação ` ${person.name} tem ${person.age} anos.`   */
console.log(` ${person.name} tem ${person.age} anos e ${person.weight} quilos.`)